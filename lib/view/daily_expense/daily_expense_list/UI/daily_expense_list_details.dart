import 'package:flutter/material.dart';

class DailyExpenseListDetails extends StatelessWidget {
  const DailyExpenseListDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
